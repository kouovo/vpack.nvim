*vpack.txt*           For Neovim >= 0.12.0          Last change: 2026 April 06

==============================================================================
Table of Contents                                    *vpack-table-of-contents*

1. vpack.nvim                                               |vpack-vpack.nvim|
  - Requirements                               |vpack-vpack.nvim-requirements|
  - Features                                       |vpack-vpack.nvim-features|
  - Install                                         |vpack-vpack.nvim-install|
  - Setup                                             |vpack-vpack.nvim-setup|
  - Usage                                             |vpack-vpack.nvim-usage|
  - Keymaps                                         |vpack-vpack.nvim-keymaps|
  - Notes                                             |vpack-vpack.nvim-notes|
  - Help                                               |vpack-vpack.nvim-help|
2. Links                                                         |vpack-links|

==============================================================================
1. vpack.nvim                                               *vpack-vpack.nvim*

Small UI for Neovim 0.12 `vim.pack`.

`vpack.nvim` gives you a simple `:Vpack` window for inspecting and operating on
plugins managed by the built-in pack API.


REQUIREMENTS                                   *vpack-vpack.nvim-requirements*

- Neovim >= 0.12
- plugins managed through `vim.pack`


FEATURES                                           *vpack-vpack.nvim-features*

- list managed plugins
- show current revision and source/path details
- update current plugin
- update all plugins
- delete current plugin
- clean non-active plugins with `X`
- open `nvim-pack.log`
- auto-refresh on `PackChanged`


INSTALL                                             *vpack-vpack.nvim-install*


VIM.PACK ~

>lua
    vim.pack.add({
      { src = "https://github.com/kouovo/vpack.nvim" },
    })
<


SETUP                                                 *vpack-vpack.nvim-setup*

>lua
    require("vpack").setup({
      window = {
        border = "rounded",
        width = 0.8,
        height = 0.8,
      },
      log = {
        path = vim.fs.joinpath(vim.fn.stdpath("log"), "nvim-pack.log"),
        border = "rounded",
        width = 0.8,
        height = 0.6,
      },
    })
<


USAGE                                                 *vpack-vpack.nvim-usage*

Open the UI with:

>vim
    :Vpack
<


KEYMAPS                                             *vpack-vpack.nvim-keymaps*

Inside the `:Vpack` window:

- `<CR>`: toggle details for current plugin
- `r`: refresh
- `u`: update current plugin
- `U`: update all plugins
- `d`: delete current plugin
- `X`: clean non-active plugins
- `l`: open `nvim-pack.log`
- `q`: close

Inside the log window:

- `G`: jump to bottom
- `q`: close


NOTES                                                 *vpack-vpack.nvim-notes*

- `delete` removes the package from disk; it does not edit your config.
- `clean` removes all non-active packages currently reported by `vim.pack.get()`.
- already-loaded plugins may still affect the current session until restart.
- colors follow your existing highlight groups.


HELP                                                   *vpack-vpack.nvim-help*

This repo ships generated vimdoc as `doc/vpack.txt`. After updating helptags,
use:

>vim
    :help vpack
<

==============================================================================
2. Links                                                         *vpack-links*

1. *vpack screenshot*: ./assets/vpack.png

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
